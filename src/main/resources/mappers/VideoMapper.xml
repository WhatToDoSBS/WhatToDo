<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.koreait.whattodo.video.VideoMapper">
    <insert id="insertBoxOffice">
        INSERT INTO video_movie(boxofficeType, showRange, rank, movieNm, movieCd) VALUES
        <foreach collection="list" open="(" close=")" separator="),(" item="movie">
            #{movie.boxofficeType}, #{movie.showRange},#{movie.rank},#{movie.movieNm},#{movie.movieCd}
        </foreach>
    </insert>
</mapper>